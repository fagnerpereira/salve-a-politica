<%= form_with(model: proposal, local: true) do |form| %>
  <% if proposal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proposal.errors.count, "error") %> prohibited this proposal from being saved:</h2>

      <ul>
      <% proposal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :proposal_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age, id: :proposal_age %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :proposal_state %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :proposal_city %>
  </div>

  <div class="field">
    <%= form.label :part %>
    <%= form.text_field :part, id: :proposal_part %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
